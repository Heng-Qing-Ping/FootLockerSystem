<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Login | FootLocker</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: url('/images/hero-bg.jpg') center/cover no-repeat fixed;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: 'Segoe UI', sans-serif;
    }

    .auth-box {
      background-color: rgba(255, 255, 255, 0.1);
      padding: 2.5rem;
      border-radius: 20px;
      backdrop-filter: blur(12px);
      box-shadow: 0 10px 30px rgba(0,0,0,0.3);
      color: white;
      width: 100%;
      max-width: 1000px;
    }

    .auth-box h2 {
      font-weight: 600;
    }

    .form-control {
      background: rgba(255, 255, 255, 0.1);
      border: none;
      color: white;
    }

    .form-control::placeholder {
      color: #ccc;
    }

    label {
      font-weight: 500;
    }

    .btn-nike {
      background-color: #111;
      color: white;
      border-radius: 30px;
    }

    .btn-nike:hover {
      background-color: #333;
    }

    .alert {
      background-color: rgba(255, 0, 0, 0.8);
      border: none;
    }
  </style>
</head>
<body>
  <div class="auth-box">
    <div class="row">
      <div class="col-md-6">
        <h2 class="mb-3">Login</h2>

        <% if (error && error.length > 0) { %>
          <div class="alert alert-danger"><%= error[0] %></div>
        <% } %>

        <% if (success && success.length > 0) { %>
          <div class="alert alert-success bg-success text-white"><%= success[0] %></div>
        <% } %>

        <form action="/login" method="POST">
          <div class="mb-3">
            <label>Username</label>
            <input type="text" name="username" class="form-control" required placeholder="Enter your username">
          </div>
          <div class="mb-3">
            <label>Password</label>
            <input type="password" name="password" class="form-control" required placeholder="Enter your password">
          </div>
          <button type="submit" class="btn btn-nike w-100">Login</button>
        </form>
      </div>

      <div class="col-md-6 mt-4 mt-md-0">
        <h2 class="mb-3">Register</h2>
        <form action="/register" method="POST">
          <div class="mb-3">
            <label>Username</label>
            <input type="text" name="username" class="form-control" required placeholder="Choose a username">
          </div>
          <div class="mb-3">
            <label>Email</label>
            <input type="email" name="email" class="form-control" required placeholder="Enter your email">
          </div>
          <div class="mb-3">
            <label>Password</label>
            <input type="password" name="password" class="form-control" required placeholder="Create a password">
          </div>
          <button type="submit" class="btn btn-success w-100">Register</button>
        </form>
      </div>
    </div>
  </div>
</body>
</html>
